{
  "name": "scorestream-stream",
  "version": "0.0.1",
  "description": "ScoreStream stream manager — Puppeteer + ffmpeg per scoreboard",
  "main": "manager.js",
  "scripts": {
    "start": "node manager.js"
  },
  "dependencies": {
    "puppeteer-core": "^22.0.0",
    "better-sqlite3": "^9.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
