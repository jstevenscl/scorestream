# Environment — never commit credentials
.env
!.env.example

# Runtime data
config/config.json.bak
*.log

# Node
node_modules/
renderer/node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Docker build artifacts
.docker/

# OS
.DS_Store
Thumbs.db

# Local assets — not for repo
Logo/

# Database + runtime state — created by the container at runtime, never commit
config/scorestream.db
config/scorestream.db-shm
config/scorestream.db-wal

# Legacy root-level duplicates (active copies live in stream/ and data branch)
/manager.js
/motor_cache.json

# Screenshots and reference images shared with Claude — never commit
Screenshot*.png
Screenshot*.jpg
*.screenshot.png
screenshots/
# But docs/screenshots/ are documentation assets — always commit
!docs/screenshots/
!docs/screenshots/**

# Reference / scratch files that are NOT part of the project
# (data dumps, error logs, handoff notes, ad-hoc scripts)
*Handoff*.md
ScoreStream_Handoff*.md
error logs.txt
error_logs.txt
*.error.log
2026*.csv
Dispatcharr API.yaml
rename_cache_keys.py
seed_motor_cache.py
scoreboard_fixed.html
*_fixed.html
*_backup.html
*.bak
